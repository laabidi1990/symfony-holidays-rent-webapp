{% extends 'base.html.twig' %}

{% block title %}mise à jours du mot de passe{% endblock %}

{% block body %}
    <div class="container">
        <h1>Mise à jours du mot de passe</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary">Enregistrer</button>
        {{ form_end(form) }}
    </div>

{% endblock %}